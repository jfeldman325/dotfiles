add_newline = true
format="""
╭─$user@$hostname($local_ip):$directory ($kubernetes) $git_branch 
| Status 
╰─$time──➤ 
"""

[kubernetes]
format = "[$context:$namespace](blue) "
disabled = false

[time]
use_12hr=true
format='🌏[$time](white)'
time_format="%I:%M%p-%A %B %d %Y"
utc_time_offset = "-5"
disabled = false

[username]
style_user = "white"
style_root = "red bold"
format = "[$user]($style)[@](blue bold)"
disabled = false

[directory]
format = "[$path]($style) [$read_only]($read_only_style) "

# \uFE0E forces character to display as unicode glyph not emoji
read_only = "🔒\uFE0E"

[localip]
ssh_only=false
format = "@[$localipv4](bold red) "
disabled = false

[hostname]
ssh_only = false
format =  "[$hostname](green) "

[character]
success_symbol = "[»](bold green) "
error_symbol = "[✗](bold red) "
vicmd_symbol = "[«](bold green) "

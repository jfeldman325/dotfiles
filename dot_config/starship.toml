add_newline = true
format="""
╭─ $container$username@$hostname|$localip|$kubernetes|$directory$git_branch 
│ (underline bold blue)Status
│ local $memory_usage cpu-usage: ${custom.cpu_usage}
╰─$time──➤ 
"""
[custom.cpu_usage_low]
command="mpstat  | grep all | awk '{print 100-$12}'"
when="mpstat  | grep all | awk -v x=5 '100-$12 < x{print 0}'"
format = "[$output](white) %"

[custom.cpu_usage_low]
command="mpstat  | grep all | awk '{print 100-$12}'"
when="mpstat  | grep all | awk -v x=5 '100-$12 > x{print 0}'"
format = "[$output](bold red) %"


[kubernetes]
format = "[$context](white):[$namespace](bold red)"
disabled = false

[time]
use_12hr=true
format='🌏[$time](white)'
time_format="%I:%M%p-%A %B %d %Y"
utc_time_offset = "-5"
disabled = false

[username]
style_user = "white bold"
style_root = "red bold"
format = "user:[$user]($style)"
disabled = false
show_always = true

[memory_usage]
disabled=false
threshold=0
format="$symbol[${ram}( \\\\| ${swap})]($style) "

[directory]
format = "[$path]($style)[$read_only]($read_only_style)"

# \uFE0E forces character to display as unicode glyph not emoji
read_only = "🔒\uFE0E"

[localip]
ssh_only=false
format = "[$localipv4](bold blue)"
disabled = false

[hostname]
ssh_only = false
format = "[$hostname](green)"

[character]
success_symbol = "[»](bold green)"
error_symbol = "[✗](bold red) "
vicmd_symbol = "[«](bold green)"
